package bmiEx;

public class VarEx {

	public static void main(String[] args) {
		int value = 0;
		
		// == 비교 연산자
		if(value == 0) { //value 값이 0이면 코드블럭을 실행해라
			
			int value2 = value + 20;
			
		}
		
		int sum = value;
	//  int sum2 = value2; //코드블럭 안에서 선언된 변수는 코드블럭 밖에서 참조할 수 없다
	}
	
	
	//  int sum= vlaue; //코드블럭 안에서 선언된 변수는 코드블럭 밖에서 참조할 수 없다

}
